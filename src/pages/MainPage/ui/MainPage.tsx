import classes from './MainPage.module.scss'
const MainPage = () => {
    return (
        <main className={classes.main}>
            Главная страница
        </main>
    );
};

export default MainPage;
